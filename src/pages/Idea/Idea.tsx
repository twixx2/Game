import { Page } from '@shared/ui';

export const IdeaPage = () => {
    return (
        <Page title="Idea" subtitle='input for the future'>
            <h2 className="maintenance_title">
                currently, idea page is unavailable
            </h2>
        </Page>
    );
};