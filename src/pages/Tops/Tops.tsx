import { Page } from '@shared/ui';

export const TopsPage = () => {
    return (
        <Page title="tops" subtitle="who am i">
            <h2 className="maintenance_title">
                currently, tops page is unavailable
            </h2>
        </Page>
    );
};