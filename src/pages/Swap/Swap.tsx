import { Page } from '@shared/ui';

export const SwapPage = () => {
    return (
        <Page title="swap" subtitle='your stuff, new rules'>
            <h2 className="maintenance_title">
                currently, swaps is unavailable
            </h2>
        </Page>
    );
};