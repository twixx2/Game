import './Inv.scss';

const Inv = () => {
    return (

        <section className="inv">
            <h2 className="inv_title">
                currently, inv page is unavailable. you can check it on profile
            </h2>
        </section>

    );
};

export default Inv;