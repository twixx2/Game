import './Store.scss';

const Store = () => {
    return (

        <section className="store">
            <h2 className="store_title">
                currently, store page is unavailable
            </h2>
        </section>

    );
};

export default Store;

