import './Support.scss';

const Support = () => {
    return (

        <section className="support">
            <h2 className="support_title">
                currently, support page is unavailable
            </h2>
        </section>

    );
};

export default Support;