import './Leaderboard.scss';

const Leaderboard = () => {
    return (

        <section className="leaderboard">
            <h2 className="leaderboard_title">
                currently, leaderboard page is unavailable
            </h2>
        </section>

    );
};

export default Leaderboard;