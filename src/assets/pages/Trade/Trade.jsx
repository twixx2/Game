import './trade.scss';

const Trade = () => {
    return (

        <section className="trade">
            <h2 className="trade_title">
                currently, trade page is unavailable
            </h2>
        </section>

    );
};

export default Trade;